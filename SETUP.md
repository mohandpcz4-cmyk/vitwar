# خطوات التشغيل (لازم تعملها مرة واحدة بس)

الموقع دلوقتي بقى فيه فرعين (تجمع اول / تجمع خامس)، وأي تعديل من الأدمن (منيو، حالة
الفرع مفتوح/مغلق/مشغول) بيوصل لحظيًا لكل اللي فاتحين الموقع من أي جهاز، والطلبات
بتوصلك على بوت تليجرام أوتوماتيك. عشان ده يشتغل، لازم تربط الموقع بحساب Firebase
مجاني بتاعك (5 دقايق بس)، وتعمل بوت تليجرام.

---

## 1) اعمل مشروع Firebase مجاني

1. روح https://console.firebase.google.com وسجل دخول بحساب Google بتاعك.
2. دوس **Add project** واديله اي اسم زي `vitwar` واستمر (تقدر تلغي Google Analytics).
3. جوه المشروع، من الصفحة الرئيسية دوس على أيقونة **`</>`** (Web) عشان تسجل تطبيق ويب.
4. اديله اسم زي `vitwar-web` ودوس Register app. هيديك كود فيه كائن `firebaseConfig`
   شكله كده:
   ```js
   const firebaseConfig = {
     apiKey: "AIza...",
     authDomain: "vitwar-xxxx.firebaseapp.com",
     projectId: "vitwar-xxxx",
     storageBucket: "vitwar-xxxx.appspot.com",
     messagingSenderId: "...",
     appId: "..."
   };
   ```
5. افتح ملف **`js/firebase-config.js`** في مجلد الموقع، واستبدل القيم الوهمية بالقيم
   اللي فيربيز اداهولك بالظبط.

## 2) فعّل قاعدة البيانات Firestore

1. من القائمة الجانبية: **Build → Firestore Database → Create database**.
2. اختار **Start in production mode** ثم اختار location قريب (زي `eur3`).
3. روح تبويب **Rules** فوق، وامسح اللي موجود والصق القواعد دي بالظبط، وبعدين دوس **Publish**:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isBranchAdmin(branchId) {
      return request.auth != null &&
        request.auth.token.email == ('vitwar' + branchId + '@vitwar.local');
    }

    match /branches/{branchId} {
      allow read: if true;
      allow write: if isBranchAdmin(branchId);

      match /orders/{orderId} {
        allow read: if isBranchAdmin(branchId);
        allow create: if true;
        allow update, delete: if isBranchAdmin(branchId);
      }

      match /messages/{messageId} {
        allow read: if true;
        allow create, update, delete: if isBranchAdmin(branchId);
      }

      match /fcmTokens/{tokenId} {
        allow read: if false;
        allow create, update: if true;
        allow delete: if isBranchAdmin(branchId);
      }

      match /chats/{chatId} {
        allow read: if true;
        allow create: if true;
        allow update: if true;
        allow delete: if isBranchAdmin(branchId);

        match /messages/{messageId} {
          allow read: if true;
          allow create: if true;
          allow update, delete: if isBranchAdmin(branchId);
        }
      }
    }

    match /counters/{docId} {
      allow read, write: if true;
    }

    match /settings/{docId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    match /reviews/{reviewId} {
      allow read: if true;
      allow create: if request.resource.data.name is string &&
        request.resource.data.name.size() > 0 &&
        request.resource.data.name.size() < 60 &&
        request.resource.data.comment is string &&
        request.resource.data.comment.size() > 0 &&
        request.resource.data.comment.size() < 500 &&
        request.resource.data.stars is int &&
        request.resource.data.stars >= 1 &&
        request.resource.data.stars <= 5;
      allow update, delete: if false;
    }
  }
}
```

## 3) فعّل تسجيل الدخول (Authentication) للأدمن

القواعد اللي فوق محتاجة كل فرع يكون ليه حساب دخول حقيقي عشان محدش يقدر يعدل
المنيو غير الأدمن بتاع الفرع ده (باسورد لوحة الأدمن العادي بس مش كفاية لحماية
البيانات لأنه مش بيتفحص من سيرفر حقيقي).

1. من القائمة الجانبية: **Build → Authentication → Get started**.
2. من تبويب **Sign-in method** فعّل **Email/Password**.
3. من تبويب **Users** دوس **Add user** وضيف الحسابين دول بالظبط (نفس البيانات
   اللي هتدخل بيها في لوحة الأدمن، بس بصيغة إيميل):

   | البريد (Email)              | كلمة المرور     | الفرع       |
   |------------------------------|------------------|--------------|
   | `vitwar1@vitwar.local`       | `mohand@vitwar`  | تجمع اول    |
   | `vitwar5@vitwar.local`       | `mohand@vitwar`  | تجمع خامس   |

   ⚠️ لازم تكتب الإيميلين والباسورد بالظبط زي ما هو موجود فوق، لأن الكود مبرمج
   عليهم بالظبط. اليوزر اللي هتدخل بيه في شاشة لوحة الأدمن يفضل زي ما هو
   (`vitwar1` / `vitwar5`) — الإيميل ده بس داخلي لفايربيز ومش هيظهر للعميل.

## 3ب) فعّل تسجيل الدخول بـ Google للعملاء (اختياري)

في صفحة العميل فيه زرار "تسجيل الدخول بـ Google" جوه قائمة ☰. عشان يشتغل:

1. من نفس صفحة **Authentication → Sign-in method** اللي فتحتها فوق، فعّل
   provider اسمه **Google** (دوس عليه → Enable → اختار إيميل دعم للمشروع → Save).
2. لو الموقع هيتفتح من دومين تاني غير `localhost` أو دومين Firebase الافتراضي
   (زي دومين مخصص بتاعك)، ضيفه في **Authentication → Settings → Authorized domains**.
3. كده وبس — الزرار هيفتح نافذة تسجيل الدخول بجوجل تلقائيًا، وهيبقى اسم
   العميل ظاهر بدل الزرار بعد ما يسجل، ويقدر يسجل خروج من نفس المكان.

لو الخطوة دي متعملتش، الزرار هيظهر رسالة "تسجيل الدخول مش متاح دلوقتي" بدل
ما يعلق الموقع.

## 4) اعمل بوت تليجرام يستقبل الأوردرات

1. افتح تليجرام وابحث عن **BotFather**.
2. ابعتله `/newbot` واتبع الخطوات (اسم + username للبوت).
3. هيديك **Bot Token** شكله زي: `123456789:AAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`.
4. ابعت أي رسالة للبوت بتاعك (أو لجروب لو عايز الأوردرات توصل لجروب).
5. عشان تعرف الـ **Chat ID**:
   - لو بوت شخصي: ابحث عن `@userinfobot` في تليجرام وابعتله أي رسالة، هيديك
     الـ Chat ID بتاعك.
   - أو افتح في المتصفح:
     `https://api.telegram.org/bot<التوكن بتاعك>/getUpdates`
     بعد ما تبعت رسالة للبوت، وهتلاقي `"chat":{"id":123456789...}` في الرد.
6. سجل دخول لوحة الأدمن (أي فرع) → تبويب **الإعدادات** → حط الـ Bot Token
   والـ Chat ID → احفظ. من هنا كل أوردر جديد من أي الفرعين هيوصلك رسالة بيه.

⚠️ **ملاحظة أمان مهمة**: بما إن الموقع ملفات ثابتة من غير سيرفر خاص بينا، رسالة
تليجرام بتتبعت مباشرة من متصفح العميل، يعني توكن البوت بيبقى ظاهر في طلبات
الشبكة (Network) لو حد فتح أدوات المطور. الخطر الوحيد إن حد يقدر يبعت رسائل
للبوت بتاعك بنفس التوكن ده، مش إنه يوصل لحاجة تانية حساسة. لو حابب حماية أقوى
100%، الحل الوحيد إنك تعمل سيرفر بسيط (Cloud Function مجانية من فايربيز نفسه)
يبعت الرسالة بدل المتصفح — قولّي لو عايز ده وهظبطهولك.

## 5) لينكات وعناوين الفروع

عناوين الفروع وأسماء المستخدمين مكتوبين في ملف **`js/firebase-init.js`** في
جزء `BRANCHES`. تقدر تعدل النص (الموقع/العنوان) من هناك في أي وقت.

بعد كل أوردر، بيتفتح واتساب أوتوماتيك على رقم الفرع بتاع الطلب (رقم ثابت لكل
فرع، في نفس جزء `BRANCHES` خانة `whatsapp`):
- تجمع اول: `01066403999`
- تجمع خامس: `01080802914`

## 6) نقطة مهمة عن الصور

Firestore بيحط حد أقصى **1 ميجابايت** لحجم "المستند" الواحد، والمنيو كله (كل
الأصناف) بيتخزن في مستند واحد لكل فرع. لو رفعت صور كتير بخاصية "ارفع صورة" من
لوحة الأدمن (بتتحول لنص base64 كبير)، ممكن توصل للحد ده بسرعة والحفظ يفشل.
الأفضل دايمًا: استخدم **"اكتب مسار صورة"** بدل الرفع المباشر — يعني ارفع الصورة
مرة واحدة على استضافة صور مجانية (زي imgbb.com) أو حطها في فولدر `images/`
بتاع الموقع، وبعدين اكتب مسارها في الخانة التانية.

## 7) رقم كود الطلب (#184 وما بعده)

الرقم مشترك بين الفرعين ومتخزن في مستند `counters/global` في Firestore،
وبيزيد تلقائيًا مع كل أوردر من أي الفرعين (زي ما اتفقنا: أول رقم #184).

## 8) شات الدعم (تواصل مع الدعم)

لو كنت عملت خطوة الـ Firestore Rules فوق قبل كده على مشروع شغال بالفعل، لازم
ترجع لتبويب **Rules** في Firestore وتحدّث القواعد بنفس النص الجديد اللي فوق
(بقى فيه جزء `chats` إضافي)، وتضغط **Publish** تاني — عشان زرار "تواصل مع
الدعم" في صفحة العميل يشتغل ويتزامن مع لوحة الأدمن. لو مش هتربط Firebase
خالص، الشات هيشتغل محليًا بالمتصفح بس (تجربة بسيطة من غير تزامن حقيقي بين
الأجهزة)، بالظبط زي باقي مميزات الموقع.

## 9) إشعارات Push حقيقية (تشتغل حتى لو التطبيق مقفول)

من غير الخطوات دي، رسائل تبويب "رسائل" بتوصل بس للعملاء اللي فاتحين الموقع/
التطبيق فعليًا في نفس اللحظة. عشان توصلهم إشعار حتى لو التطبيق مقفول خالص،
محتاج جزئين: مفتاح Web Push من فايربيز، وCloud Function صغيرة (فيها free tier
سخي جدًا وهتفضل مجانية إلا لو عندك آلاف الرسائل يوميًا).

### أ) مفتاح الـ Web Push (VAPID)

1. من Firebase Console: أيقونة الترس ⚙️ (جنب "Project Overview") →
   **Project settings**.
2. تبويب **Cloud Messaging** فوق.
3. تحت قسم **Web configuration** → **Web Push certificates** دوس
   **Generate key pair**.
4. هيديك مفتاح طويل (Key pair) — انسخه وحطه في ملف **`js/firebase-config.js`**
   مكان `PASTE_YOUR_VAPID_KEY_HERE` في السطر بتاع `VAPID_KEY`.

### ب) رفع الـ Cloud Function

عشان الفنكشن تشتغل، لازم تفعّل خطة **Blaze** (Pay as you go) للمشروع — ده شرط
فايربيز نفسه لأي Cloud Function حتى لو مش هتستخدم فلوس، لأنها بتتطلب ربط وسيلة
دفع. برضو فيه free tier شهري (2 مليون استدعاء واستضافة مجانية) هيغطي الاستخدام
العادي بالكامل من غير أي خصم:

1. من القائمة الجانبية تحت Build دوس **Functions**، وهيطلب منك تفعّل خطة
   Blaze — دوس **Upgrade** واربط بطاقة (مش هتتخصم منها حاجة إلا لو عديت الحد
   المجاني الكبير).
2. سطّب **Node.js** (نسخة 20 أو أحدث) و **Firebase CLI** على جهازك لو
   مش متسطبين:
   ```
   npm install -g firebase-tools
   ```
3. افتح Terminal/CMD جوه مجلد الموقع (اللي فيه فولدر `functions` الجاهز)
   وسجل دخول:
   ```
   firebase login
   ```
4. اربط المشروع (استبدل `vitwar-xxxx` بالـ Project ID بتاعك، موجود في
   `js/firebase-config.js`):
   ```
   firebase use --add
   ```
   واختار المشروع بتاعك من القائمة.
5. ادخل فولدر `functions` وسطّب المكتبات:
   ```
   cd functions
   npm install
   cd ..
   ```
6. ارفع الفنكشن:
   ```
   firebase deploy --only functions
   ```
   لو خلص من غير أخطاء، يبقى تمام — من دلوقتي أي رسالة تبعتها من تبويب
   "رسائل" في لوحة الأدمن هتتبعت push حقيقي لكل الأجهزة اللي فاتحة الموقع
   وأخدت إذن الإشعارات، حتى لو التطبيق مقفول خالص.

### ج) تحديث قواعد Firestore

القواعد الجديدة فوق (خطوة 2) بقى فيها جزء `fcmTokens` إضافي. لو كنت نسخت
القواعد على مشروع شغال قبل ما تضيف الجزء ده، ارجع لتبويب **Rules** في
Firestore والصق النسخة الجديدة كاملة، ودوس **Publish** تاني.

### د) لو حبيت تعدّل الفنكشن بعدين

كل كود الفنكشن في ملف **`functions/index.js`** (وسطرين إعداد في
`functions/package.json`). أي تعديل تعمله فيه، لازم تعمل تاني:
```
firebase deploy --only functions
```
عشان التعديل ينزل فعليًا على السيرفر.

⚠️ **ملاحظة**: بعض المتصفحات (خصوصًا Safari على آيفون) بتدعم Web Push بس لو
الموقع متثبت كـ "Add to Home Screen" (PWA) — ده أصلًا نفس اللي الموقع بيشجع
عليه من زرار التثبيت الموجود.

## 10) حماية المحتوى من النسخ

الموقع بقى فيه ملف **`js/protect.js`** (مربوط في `index.html` بس، صفحة العميل)
بيمنع: تحديد/نسخ النص، حفظ أو سحب الصور، وبيحط علامة مائية شفافة فوق صور
المنتجات، وبيعتّم المحتوى تلقائيًا لو حس إن أدوات المطور (DevTools) فاتحة.

⚠️ **مهم تعرفه**: دي أقصى حماية ممكنة بتقنيات الويب، لكن **مفيش طريقة توقف
سكرين شوت حقيقي** (زرار الموبايل أو الـ Print Screen) لأنه تحكم نظام التشغيل
نفسه مش المتصفح — حتى التطبيقات الكبيرة اللي بتمنع السكرين شوت فعليًا (زي
نتفليكس) لازم تكون تطبيق Native مثبت من المتجر، مش موقع/PWA زي بتاعك.

لو عايز تغيّر النص اللي بيظهر فوق الصور (العلامة المائية)، هتلاقيه في
**`css/style.css`** جوه `background-image` بتاع `.img-wrap::after` — دور على
كلمة `Vitwar` جوه الـ SVG وغيّرها.

## 11) قسم "الرسائل" عند العميل (جوه قائمة الـ☰)

أي رسالة تبعتها من لوحة الأدمن (تبويب "رسائل") بقى ليها 3 حاجات بتحصل مع بعض
عند العميل:

1. **بانر لحظي** يظهر فوق الموقع بشكل جميل لو العميل فاتح الموقع وقتها،
   ويختفي لوحده بعد 7 ثواني (زي ما كان بالظبط قبل كده).
2. **صوت تنبيه** بيتشغل تلقائيًا من ملف **`sounds/notification.mp3`**
   (دالة `playNotificationSound()` في `js/toast.js`، مستدعاة من
   `js/main.js` جوه `startBroadcastListener`).
3. **تخزين دائم**: الرسالة بتتحفظ في قسم **"الرسائل"** الجديد جوه قائمة
   الـ☰ (التلات خطوط) عند العميل — أيقونة 🔔 — فيها كل الرسائل اللي
   اتبعتت للفرع ده، بالتاريخ، وتفضل موجودة حتى لو العميل قفل الموقع ورجع
   تاني. لو فيه رسالة جديدة لسه ما فتحهاش، بتظهر نقطة حمرا صغيرة فوق
   زرار "الرسائل" في القائمة، وبتختفي أول ما يفتح القسم.

مفيش أي تعديل مطلوب على قواعد Firestore هنا — القسم بيستخدم نفس الـ
collection القديمة `branches/{branchId}/messages` اللي أصلًا فيها
`allow read: if true`، فأي عميل يقدر يقراها عادي من غير تسجيل دخول.

الكود كله موجود في **`js/main.js`** (دوال `renderCustomerMessagesInbox`،
`updateMessagesBadge`، `markMessagesAsSeen`) والمودال نفسه في
**`index.html`** (بحث عن `customerMessagesModal`)، والتنسيق في
**`css/style.css`** (بحث عن `.inbox-`).

## 12) التقييمات (Rate us) في شاشة اختيار الفرع

فوق "اختيار الفرع" جنب اللوجو، فيه شارة زجاجية `✩ 4.9 · 764 reviews`، ولما
العميل يدوسها بتفتح مودال فيه: ملخص التقييم (4.9 ونسب النجوم 60% خمس نجوم /
35% أربع نجوم / 5% ثلاث نجوم)، قائمة تقييمات، وفورم "قيّمنا" (نجوم + اسم +
تعليق) يقدر العميل يبعت بيه تقييمه هو.

- الرقمين **4.9** والـ **764** ونسب النجوم دي أرقام ثابتة (زي ما اتفقنا)، مش
  بتتغير تلقائيًا مع كل تقييم جديد يوصل.
- التقييمات اللي ظاهرة في القائمة جزء منها تجريبي (Seed) ثابت في الكود عشان
  الشاشة متبقاش فاضية، وجزء تاني هيتضاف فعليًا لما عميل يبعت تقييم من عنده.
- **لو فايربيز شغال (خطوة 2 فوق متعملة)**: تقييم العميل بيتحفظ في كولكشن
  عامة اسمها `reviews` في فايربيز (مش تابعة لفرع معين، مشتركة بين كل
  الفروع)، والموقع بقى **يتابعها لحظيًا (live)**، يعني أي تقييم جديد من أي
  عميل على أي جهاز بيبان فورًا لكل العملاء التانيين من غير ما حد يعمل
  Refresh — ده كان قبل كده بيتحفظ بس في `localStorage` بتاع جهاز العميل نفسه
  ومكانش بيبان لغيره، وده اللي اتصلح.
- **من غير فايربيز (أو لو الجدول مش متظبط)**: الموقع بيرجع تلقائي للسلوك
  القديم (تخزين محلي في `localStorage` بس، ميوصلش لعملاء تانيين).
- العميل نفسه مينفعش يعدّل أو يمسح تقييم بعد ما يبعته (القاعدة بتمنع
  `update`/`delete` خالص، حتى منه هو).
- لو كنت عملت خطوة الـ Rules قبل إضافة جزء `reviews` ده، لازم ترجع لتبويب
  **Rules** في Firestore وتلصق النسخة الجديدة كاملة (اللي فيها الجزء ده)
  وتدوس **Publish** تاني — زي بالظبط ما عملت مع أجزاء `chats` و`fcmTokens`
  قبل كده.

الكود كله في **`js/main.js`** (دوال `renderReviewsList`، `renderReviewsBars`،
`renderRatingBadge`، `pushReviewToFirestore`)، والمودال نفسه في
**`index.html`** (بحث عن `reviewsModal`)، والتنسيق في **`css/style.css`**
(بحث عن `.reviews-` و`.rate-us-`).

## 13) شات الدعم: رسايل ترحيب تلقائية + "بيكتب الآن..." + علامة الاتقرأت (✓/✓✓)

- **رسالتين ترحيب تلقائيتين**: أول ما عميل يبدأ محادثة دعم جديدة (يملى
  الاسم والموبايل ويدوس "ابدأ المحادثة")، بتتبعتله أوتوماتيك رسالتين من
  جانب "الدعم":
  1. "من الممكن ان نتأخر في الرد ولكن سنحاول الإجابة سريعاً"
  2. "برجاء اترك رسالتك."
  دول بيتسجلوا كرسايل عادية في المحادثة (زي أي رد أدمن)، فبيظهروا عند
  العميل والأدمن مع بعض في نفس المحادثة. الكود في `sendAutoReplyMessages`
  (`js/store.js`) ومستدعاة من `handleStartChat` في `js/chat.js`.

- **"بيكتب الآن..." (Typing indicator)**: لما أي طرف (عميل أو أدمن) يكتب في
  خانة الرسالة، بيتسجل وقت آخر ضغطة على حقل `typingCustomerAt` /
  `typingAdminAt` جوه مستند الشات نفسه في Firestore (`chats/{chatId}`)،
  والطرف التاني بيشوف فقاعة "..." متحركة بستايل واتساب لكن بألوان الموقع.
  لو الكتابة توقفت لأكتر من 2.5 ثانية أو الرسالة اتبعتت، المؤشر بيختفي
  تلقائي. مفيش أي تعديل مطلوب على قواعد Firestore — القسم ده بيستخدم نفس
  صلاحية `allow update: if true` الموجودة أصلًا على `chats/{chatId}`.

- **علامة الاتقرأت (✓ اتبعتت / ✓✓ اتقرأت)**: لما أي طرف يفتح المحادثة أو
  يشوفها، بيتسجل وقت آخر مشاهدة في `lastSeenByAdminAt` / `lastSeenByCustomerAt`
  على مستند الشات. تحت كل رسالة بعتها الطرف نفسه، بتظهر علامة تِك واحد
  (اتبعتت) أو تِكين ملونين (اتقرأت) حسب وقت الرسالة مقارنة بوقت آخر مشاهدة
  من الطرف التاني — زي واتساب بالظبط. مفيش إعداد Firestore إضافي مطلوب هنا
  كمان.

كل الكود الخاص بالتلات ميزات دي موجود في `js/store.js` (الدوال
`sendAutoReplyMessages`، `setTypingStatus`، `isTypingActive`، وتحديث
`markChatSeenByAdmin`/`markChatSeenByCustomer`)، وفي `js/chat.js` و
`js/admin.js` (منطق العرض)، والتنسيق في `css/style.css` (بحث عن
`.typing-indicator-` و`.support-msg-ticks`).

## 14) مودال "العنوان" (Location) في قائمة الـ☰ — خريطة مختلفة لكل فرع

جوه قائمة الـ☰ (التلات خطوط) اتضاف زرار جديد "📍 العنوان" (Location
بالإنجليزي)، بيفتح مودال بستايل الموقع فيه:
1. خريطة (iframe) — لينك مختلف لكل فرع.
2. عنوان الفرع كتابةً — بيتغير عربي/إنجليزي تلقائي مع لغة الموقع (بياخد
   نفس القيمة الموجودة أصلًا في `location` / `locationEn` لكل فرع).
3. "See you soon !" ثابتة تحت خالص بلون وردي، برا السكرول.

**لازم تحط لينك الخريطة بنفسك**: افتح خريطة الفرع على Google Maps، دوس
**مشاركة (Share) → تضمين خريطة (Embed a map)**، وانسخ اللينك اللي جوه
`src="..."` بس (مش كود الـ `<iframe>` كله)، والصقه في `js/firebase-init.js`
جوه `BRANCHES`، في حقل `locationMapUrl` بتاع كل فرع (سيبته فاضي دلوقتي
`""`). ممكن كمان تغيّر نص العنوان نفسه من نفس المكان (`location` /
`locationEn`) في أي وقت.

مفيش أي تعديل مطلوب على قواعد Firestore — القسم ده بيقرأ بيانات ثابتة من
`js/firebase-init.js` بس، مش من قاعدة البيانات. الكود في `js/main.js`
(دالة `renderLocationModal`)، والمودال نفسه في `index.html` (بحث عن
`locationModal`)، والتنسيق في `css/style.css` (بحث عن `.location-`).

## 15) تحسينات وضع اللايت (Light Mode) + شكل التقييمات

- **أزرار اختيار الفرع في صفحة الأدمن** كانت بتستخدم نفس تصميم صفحة
  العميل (خط أبيض على خلفية زجاج شفاف) — وده شكل مصمم عشان يقعد فوق صورة
  هيرو غامقة زي صفحة العميل. لكن في صفحة الأدمن مفيش صورة هيرو، الكارت
  أبيض عادي، فكان الخط الأبيض بيختفي. اتظبطت دلوقتي بخلفية متدرجة بلون
  الهوية (وردي/بنفسجي) بدل الزجاج الشفاف، فبقت واضحة في اللايت مود.
- **نوافذ "عننا" و"التقييمات" و"الرسائل" و"العنوان"**: بدل الخلفية
  البيضاء المسطحة، بقى فيها تدرج خفيف بألوان الهوية (وردي/بنفسجي) فوق
  خلفية الثيم، يعني شكلها بقى متماشي مع باقي الموقع بدل ما تبان "بيضاء
  فاضية".
- **كروت التقييمات (Reviews)**: اتصمم تصميم جديد — دايرة بحرف اسم العميل
  الأول، خلفية كارت واضحة، ولون النص بقى `var(--ink)` (بيتغير لون تلقائي
  حسب الثيم: غامق في اللايت، فاتح/أبيض في الدارك) بدل اللون الرمادي
  الثابت اللي كان صعب يتقرا.
- **علامة التوثيق (✓) في شات الدعم**: كانت بتتقلب لدايرة بيضاء جوه هيدر
  الشات، دلوقتي بقت دايرة وردية ثابتة في كل مكان (لايت/دارك/جوه شات
  الدعم) زي باقي الموقع.
- **عداد التقييمات (764 → 765)**: اتأكد إنه شغال صح — أي تقييم جديد
  بيتبعت بيزود العداد فورًا (سواء فايربيز شغال أو لأ).

---
بعد الخطوات دي كلها، ارفع مجلد الموقع زي ما هو (بنفس أسماء الملفات) على
الاستضافة بتاعتك، وهيشتغل عادي. فولدر **`functions`** و**`firebase.json`**
مش من ملفات الموقع نفسه (مش بترفعهم على الاستضافة العادية) — دول بس بيترفعوا
لفايربيز بالـ `firebase deploy` زي ما شرحنا فوق.


